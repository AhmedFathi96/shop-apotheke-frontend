import repositories from "../slices/repositories/repositoriesSlice";
import tableControls from "./tableControls/tabelControlsSlice";

const reducers = {
  repositories,
  tableControls
};

export default reducers;
